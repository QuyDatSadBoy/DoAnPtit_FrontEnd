# ===========================================
# Frontend Environment Configuration
# ===========================================
#
# ┌─────────────────────────────────────────────────────────────┐
# │                    CÁCH SỬ DỤNG                              │
# ├─────────────────────────────────────────────────────────────┤
# │                                                             │
# │  🔧 DEVELOPMENT (không qua Kong):                          │
# │  ─────────────────────────────────                         │
# │     npm start                                               │
# │                                                             │
# │     → URL: http://localhost:3000                            │
# │     → API: http://localhost:8999 (trực tiếp)               │
# │     → Env: .env.development                                 │
# │                                                             │
# │  🚀 PRODUCTION (qua Kong):                                  │
# │  ─────────────────────────────                              │
# │     npm run build:kong                                      │
# │     serve -s build -l 5000                                  │
# │                                                             │
# │     → URL: http://localhost:8000/api/v1/fe/medical/        │
# │     → API: http://localhost:8000/api/v1/medical (qua Kong) │
# │     → Env: .env.production                                  │
# │                                                             │
# └─────────────────────────────────────────────────────────────┘
#
# React tự động load env file theo NODE_ENV:
# - npm start  → NODE_ENV=development → .env.development
# - npm build  → NODE_ENV=production  → .env.production
#
# ===========================================

# ===========================================
# URL Configuration - DEVELOPMENT (mặc định)
# ===========================================

# API URL: FE gọi BE trực tiếp
REACT_APP_INTERNAL_API_URL=http://localhost:8999/api/v1/medical

# Socket URL: Socket.IO trực tiếp
REACT_APP_INTERNAL_SOCKET_URL=http://localhost:8999

# PUBLIC URL: URL để hiển thị ảnh, static files
REACT_APP_PUBLIC_BACKEND_URL=http://localhost:8999

# ===========================================
# App Configuration
# ===========================================

# App Name (displayed in UI)
REACT_APP_NAME=Medical Imaging System

# App Version
REACT_APP_VERSION=1.0.0

# ===========================================
# Feature Flags
# ===========================================

# Enable/disable debug mode (shows console logs)
REACT_APP_DEBUG=true

# Enable/disable Socket.IO real-time notifications
REACT_APP_ENABLE_SOCKET=true

# ===========================================
# File Upload Configuration
# ===========================================

# Maximum file size for X-ray upload (in MB)
REACT_APP_MAX_UPLOAD_SIZE_MB=50

# Allowed file types for X-ray upload
REACT_APP_ALLOWED_FILE_TYPES=.png,.jpg,.jpeg,.npy

# ===========================================
# UI Configuration
# ===========================================

# Default language (vi | en)
REACT_APP_DEFAULT_LANGUAGE=vi

# Items per page in tables
REACT_APP_DEFAULT_PAGE_SIZE=10

# ===========================================
# Development Server Configuration
# ===========================================

# Allow any host header (for Kong proxy)
DANGEROUSLY_DISABLE_HOST_CHECK=true

# ===========================================
# Kong Proxy Configuration (FE qua Kong)
# ===========================================

# PUBLIC_URL: Set base path cho static assets khi chạy qua Kong
# - Khi truy cập FE qua Kong: http://localhost:8000/api/v1/fe/medical
# - Các static assets (favicon, manifest.json, etc.) cần có base path này
#
# PRODUCTION: Set PUBLIC_URL trước khi build
#   PUBLIC_URL=/api/v1/fe/medical npm run build
#
# DEV: Cần thêm routes cho static assets trên Kong (xem README)
PUBLIC_URL=

# Disable source map warnings
GENERATE_SOURCEMAP=false
